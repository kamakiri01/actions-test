name: Node.js CI

on: push

env:
  NODE_VERSION: 18

jobs:
  build:

    runs-on: windows-latest
    name: test-ci
    steps:
    - uses: actions/checkout@v2
    - name: foo
      shell: /usr/bin/bash -e {0}
      run: |
        npm i
        echo $?
        npm run maybe-error
        echo $?
        ls
        echo $?
        npm run b
        echo $?