name: Node.js CI

on: push

env:
  NODE_VERSION: 18

jobs:
  build:

    shell: /usr/bin/bash -e {0}
    runs-on: windows-latest
    name: test-ci
    steps:
    - uses: actions/checkout@v2
    - name: foo
      run: |
        npm i
        echo $?
        npm run maybe-error
        echo $?
        ls
        echo $?
        npm run b
        echo $?